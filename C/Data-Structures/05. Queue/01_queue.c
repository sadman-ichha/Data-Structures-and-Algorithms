#include <stdio.h>
#include <stdbool.h>
#define MAXQUEUE 5
int queue[MAXQUEUE];
int front =0, rear =-1,totalData =0;
void enqueue(int data);
int dequeue();
int main()
{
    
    return 0;
}

bool isFull(){
    if (totalData == MAXQUEUE)
    {
        /* code */
    }
    
}
void enqueue(int data){
   if (rear== MAXQUEUE)
   {
    /* code */
   }
   
}

int dequeue(){
    return 0;
}